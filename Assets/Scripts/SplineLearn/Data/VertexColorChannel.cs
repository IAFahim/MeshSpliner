namespace SplineLearn.Data
{
    /// <summary>
    /// Defines which color channel to target for mesh painting.
    /// </summary>
    public enum VertexColorChannel : byte
    {
        Red = 0,
        Green = 1,
        Blue = 2,
        Alpha = 3
    }
}